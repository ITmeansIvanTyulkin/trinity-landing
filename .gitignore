# Local secrets / overrides (do not commit)
.env
.env.*
!.env.example

# OS / IDE
.DS_Store
Thumbs.db
.idea/
*.iml
.vscode/
*.swp
.PVS-Studio/

# Logs
*.log

# Cabinet Supabase keys (copy from cabinet-config.example.js)
js/cabinet-config.local.js

# Local scratch / verification shots (not for publish)
assets/hero-verify-after.png

# Node (if added later)
node_modules/
dist/
.cache/
